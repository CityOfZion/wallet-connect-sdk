{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish --yes",
    "clean": "lerna clean --yes",
    "setup": "lerna init && lerna bootstrap && lerna exec npm install"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.3.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
